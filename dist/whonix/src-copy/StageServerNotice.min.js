(function(){if($('head meta[name="server-type-stage"]').length){let e=$(`
			<div id="stage-server-indicator">
				<i class="fa-solid fa-s"></i>
				<i class="fa-solid fa-exclamation"></i>
				<p>This is the stage<br>server for this wiki.</p>
			</div>
		`);$("body").append(e)}})()